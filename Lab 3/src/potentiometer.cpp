/*
#include <Arduino.h>

// jcl212 - potentiometer signal is wired to A0 (GPIO26)
const int sensorPin = A0;

void setup() {
  Serial.begin(115200);
}

void loop() {
  // jcl212 - read the potentiometer (0-4095) and print it to the serial monitor
  Serial.println(analogRead(sensorPin));
  delay(50);
}
*/
